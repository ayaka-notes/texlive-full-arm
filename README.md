# Repository Moved

The repository has moved to:
https://github.com/ayaka-notes/texlive-full

We have now merged the arm64 and amd64 images and publish them using the OCI multi-architecture standard.

From now on, when you pull the image, the container runtime (Docker/Podman, etc.) will automatically select the correct architecture for your system. No manual tag selection is required.

Thank you for your support!
